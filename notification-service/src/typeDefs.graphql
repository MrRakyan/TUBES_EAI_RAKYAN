type Notification {
  id: Int
  bookingId: Int
  userId: String
  message: String
  type: String
  createdAt: String
}

type Query {
  notificationsByUser(userId: String!): [Notification]
}

type Mutation {
  createNotification(bookingId: Int!, userId: String!, message: String!, type: String!): Notification
}

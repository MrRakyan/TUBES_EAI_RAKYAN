type Movie {
  id: Int
  title: String
  genre: String
  duration: Int
  rating: Float
  price: Int
}

type Query {
  movies: [Movie]
  movieById(id: Int!): Movie
}

type Mutation {
  addMovie(
    title: String!
    genre: String!
    duration: Int!
    rating: Float!
    price: Int!
  ): Movie
}

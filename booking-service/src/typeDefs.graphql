enum BookingStatus {
  PENDING
  PAID
  CANCELLED
}

type Movie {
  id: Int
  title: String
  duration: Int
  rating: Float
  price: Int
}

type Booking {
  id: Int
  userId: String
  movieId: Int
  seatNumber: String
  totalPrice: Int
  status: BookingStatus
  movie: Movie
}

type Query {
  bookings: [Booking]
  booking(id: Int!): Booking
}

type Mutation {
  createBooking(
    userId: String!
    movieId: Int!
    seatNumber: String!
  ): Booking

  updateBookingStatus(
    id: Int!
    status: BookingStatus!
  ): Booking
}
